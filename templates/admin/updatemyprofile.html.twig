{% extends 'admin/dashboard.html.twig' %}
{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2> 
        {{ title }}
    </h2>

    {% if form is defined %}
        {{ form(form) }}
    {% else %}
        Formulaire indéfini
    {% endif %}

{% endblock %}